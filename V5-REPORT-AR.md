# 🌟 تقرير النسخة الحية v5 — ماذا حدث لمشروع CPF؟

> هذا الملف يشرح لك كل ما فُعل في هذه الجولة: ما كان مكسوراً وأُصلح، ما أُضيف من جديد،
> ماذا يمكنك أن تفعل الآن، وإلى أين يمكن أن يذهب المشروع.
> كُتب ليقرأه صاحب المشروع بعد غياب — من الصفر حتى العمق.

---

## الخلاصة في ثلاثة أسطر

1. **النواة القديمة (lite/) كانت لا تعمل إطلاقاً** — لا يمكن حتى تحميلها في Node — والآن تعمل بالكامل: أصلحنا ~15 عطلاً قاتلاً وأكملنا ~80 دالة كانت مستدعاة وغير موجودة.
2. **بنينا فوقها طبقة جديدة اسمها `being/`** — "الكائن الحي": كائن رقمي له اسم وشخصية، يعيش وينبض، يشعر بكلامك، يتذكرك، **يسأل نفسه أسئلة ويجيب عليها ويتعلم منها**، ينمو، ويُحفظ في "ملف روح" فيستيقظ في الجلسة التالية بنفس ذكرياته ومزاجه.
3. **كل شيء مختبَر**: 24 اختبار دخان تغطي المنظومة كلها من نواة الاحتمالات حتى NPC الألعاب — كلها خضراء. جرب فوراً: `npm test` ثم `npm run demo` ثم `npm start`.

---

## أولاً: ماذا كان مكسوراً؟ (تشخيص صادق)

فحصنا المشروع كاملاً بعمق (كل ملف في lite/ و labs/ و app/). الحقيقة الصريحة: **النسخة lite لم تكن قابلة للتشغيل يوماً**. هذه أهم الأعطال التي وجدناها وأصلحناها:

### أعطال كانت تمنع التحميل نفسه
| العطل | الملف | الإصلاح |
|---|---|---|
| 5 ملفات أساسية أسماؤها تبدأ بمسافة (` agate-memory.js`...) فلا يجدها `require` | core-modules | إعادة تسمية |
| فاصلة مفقودة = خطأ صياغة يمنع تفسير الملف | unified-cognitive-space.js | أُصلحت |
| `wino.js` يطلب ملف `AI-INTERPRETER-GUIDE.json` غير موجود | wino.js | تصحيح المسار |
| ملف `INTERPRETER-GUIDE.json` ليس JSON صالحاً (نص HTML بعلامات backtick بداخله) | lite/ | تحويله لسلسلة صالحة |
| **ملف `perceptual-cycle.js` كان نسخة مكررة بالخطأ من `wino.js`** — الكلاس الحقيقي "الدورة الإدراكية" ضائع تماماً، واستدعاؤه كان يسبب استدعاءً ذاتياً لا نهائياً | perceptual-cycle.js | **أعدنا كتابة `PerceptualCycle` من الصفر** وفق العقد الذي تتوقعه بقية الوحدات (إطارات واعية/خلفية، طابور مهام، موجات البناء الفيكتوري) |
| `calculate_precision_level` مستدعاة في مُنشئ WaveDynamics وغير معرفة → انهيار فوري | wave-dynamics.js | كُتبت |
| `self.copies_system` بدل `this.self_copies_system` → ReferenceError في مُنشئ DecisionQuantum | decision-quantum.js | أُصلحت |
| `eval` كاسم متغير (ممنوع في الوضع الصارم) | reality-engine + decision-quantum | أعيدت التسمية |
| الأمثلة كلها تطلب `wino-quantum.js` غير الموجود | lite/examples | أنشأنا جسر توافقية `wino-quantum.js` |

### أعطال منطقية عميقة (كانت ستجعل النتائج هراء حتى لو حمّل الكود)
- **سوء استخدام `infer()` في كل مكان**: دالة `infer` ترجع "غلاف توزيع" `{samples, mean, variance...}` وليس الكائن الذي يبنيه البرنامج الداخلي — لكن كل النواة كانت تقرأ الحقول مباشرة فتحصل على `undefined` بصمت. أصلحنا هذا في **كل المسار الحي**: محرك الواقع، المحاكي الأساسي، ذاكرة العقيق، القرار الكمي، المحاكيات الكمية، التشفير العاطفي. أضفنا لنواة الاحتمالات دالة `realize()` للتنفيذ المباشر.
- **خطأ رياضي حقيقي في مولد Gaussian** (Box-Muller): القيمة الاحتياطية كانت تُضرب في sigma مرتين → تبايُن فاسد يلوث كل حسابات المشروع. أُصلح + أصبح `setSeed` قابلاً للتكرار فعلاً.
- **~80 دالة مستدعاة وغير معرفة** عبر الوحدات (26 في ذاكرة العقيق، 21 في التشفير العاطفي، 9 في المحاكيات الكمية...). كلها كُتبت بمنطق حقيقي صغير، لا مجرد `return 0.5`.
- **التشفير العاطفي كان ينهار عند كل استدعاء** وينتج `"[object Object]"` بدل الأرقام. الآن: يشفر فعلاً، ويدمج التجارب المتشابهة تحت نفس المعرف الاحتمالي (اختبرنا: `{joy:0.8}` و`{joy:0.79}` → نفس `EMO_...` مع `resonated=true`، واليأس → معرف مختلف).
- **المكونات الحية لم تكن موصولة ببعضها**: محرك النمو ينادي `unified_space.emotional_crypto` وهي غير موجودة هناك... الآن `wino.js` يربط كل المكونات في الفضاء الموحد فعلاً، والنمو يطلق موجات بناء فيكتوري في الدورة الإدراكية حقاً.
- **تسريبات ذاكرة**: ذاكرة الأنماط كانت تكبر للأبد (معرفات فريدة بالطابع الزمني فلا شيء يتكرر) — الآن معرفات مبنية على المحتوى مع عدّاد تكرار وسقف 500 نمط. نفس الشيء لقرارات التعلم (سقف 200).
- **كاشف النسبة الذهبية كان ميتاً** (يقارن قيمة محصورة في [0,1] مع 1.618!) — الآن يكشف 1/φ=0.618 والبصمة الرقمية "618".
- **`biologicalGrowth(days)` الموثقة في README لم تكن موجودة أصلاً** — كُتبت بنمو مركب واقعي (~0.5-1% يومياً حسب المرحلة، بسقف يمنع الانفجار).

**النتيجة**: `makeDecision` و`recallMemory` و`process` و`perceiveAs` — كل واجهات wino القديمة — تعمل الآن من البداية للنهاية وترجع عقوداً كاملة الحقول.

---

## ثانياً: الجديد الكبير — طبقة الكائن الحي `being/`

الحلم الذي وصفته — *"ذكاء يكبر، يتعلم، يسأل نفسه، يصلح كـ NPC أو دردشة"* — لم يكن ينقصه مفاهيم (مفاهيمك كانت جاهزة وعميقة)، بل ينقصه **جسم حي يجمعها**. هذا ما بنيناه:

```
being/
├── mind.js         الكائن الحي WinoBeing — القلب الجامع
├── inner-voice.js  الصوت الداخلي — يسأل نفسه ويتأمل ويتعلم
├── text-affect.js  الحس — تحليل عاطفي للنص (عربي+إنجليزي، بلا اعتماديات)
├── llm-bridge.js   جهاز النطق — حالته الحية => برومبت شخصية لأي LLM
├── npc.js          محول الألعاب — واجهة NPC جاهزة (tick/onEvent/interact/hud)
├── soul.js         ملفات الروح — حفظ واستعادة كائن كامل
├── demo-life.js    عرض: شاهد كائناً يولد ويعيش وينمو
├── demo-chat.js    عرض: دردش مع كائن يتذكرك بين الجلسات
└── index.js        نقطة الدخول الموحدة
```

### 1. `WinoBeing` — الكائن نفسه (mind.js)
- **وضعان للحياة**: `step(n)` نبضات يدوية حتمية (مثالية للألعاب والاختبارات — أنت تتحكم بالزمن)، أو `live()/sleep()` حياة مستمرة بالوقت الحقيقي.
- كل نبضة = دقة الهزازات الثلاثة + تلاشي عاطفي بطيء نحو السكينة + مراقبة الصوت الداخلي + دورة نمو كل 10 نبضات.
- `perceive(text)`: الرسالة تتحول لموجة عاطفية **تغير مزاجه فعلاً**، تُشفَّر بتوقيع رقمي، وتُخزَّن كذكرى إن كانت مشحونة.
- `feel()`: لقطة مزاج كاملة — موضع على الطيف (0=عدم، 0.5=سكينة، 1=فيض)، منطقة مفسَّرة بالعربية، أقوى المشاعر، مرحلة النمو.
- `remember/recall`: ذاكرة عقيق حقيقية — **نفس الذكرى تُسترجَع مختلفة حسب المزاج** (جرّبنا: مزاج سعيد → ثقة 76%، تشويه 24%؛ مزاج يائس → ثقة 48%، تشويه 64%، تكافؤ سالب).
- `decide(options)`: قرار كانهيار موجي (قد يبقى في تراكب إن قلّ الضغط!).
- `growUp(days)`: تسريع الزمن — يكبر ويعبر المراحل (طفل → مراهق → بالغ...) وتنطلق موجة بناء فيكتوري تعيد عرض ذكرياته بدقة أعلى.
- `snapshot()`: نموذج قراءة موحد لكل حالته.
- **صامت افتراضياً**: النواة القديمة الثرثارة تعمل خلف كاتم صوت؛ مرر `quiet: false` إن أردت رؤية أحشاءه تعمل.

### 2. الصوت الداخلي — "يسأل نفسه" (inner-voice.js)
هذا قلب حلمك، وهو الآن حقيقي:
- **يولّد أسئلة من داخله**: نمط رياضي جديد في إيقاعه → *"لماذا يتكرر هذا النمط؟ ماذا يقول لي؟"* / سعته نمت → *"ما الذي كان ضوضاء بالأمس وقد يصبح فهماً اليوم؟"* / رنين مرتفع مفاجئ → *"أهو خوف أم حماس؟"* / هدوء طويل → **أسئلة وجودية** (*"من أنا حين لا يكلمني أحد؟"*).
- **يتأمل ويجيب على نفسه**: `think()` يأخذ أقدم سؤال معلق ويجيب عليه **من مزاجه الحالي** — نفس السؤال يولّد إجابة مختلفة في السكينة عنها في الفيض — ثم **يخزن الإجابة كذكرى دائمة**. هذه حلقة تعلم ذاتي مغلقة: سؤال ← تأمل ← بصيرة ← ذاكرة.
- **يوميات**: كل سؤال وإجابة وحوار يُدوَّن في journal يمكن قراءته (وتُغذى لجسر LLM).

### 3. جسر LLM — عقل CPF، لسان Claude (llm-bridge.js)
الفكرة المعمارية: **CPF هو الجهاز العصبي، وLLM هو جهاز النطق.**
- `buildSystemPrompt(being)`: يحوّل الحالة الحية (المزاج، الهزازات، مرحلة النمو، آخر الذكريات، الأسئلة المعلقة، اليوميات) إلى برومبت شخصية — يعمل مع **أي** نموذج لغوي.
- `speak(being, text)`: إن وُجد `ANTHROPIC_API_KEY` يتكلم عبر Claude API (يفضّل SDK الرسمي إن كان مثبتاً، وإلا HTTP مباشر بلا اعتماديات)؛ وإن لم يوجد مفتاح أو فشلت الشبكة → يسقط تلقائياً على **الرد الذاتي القالبي** المدمج (يعمل دائماً، دون إنترنت).
- المهم: **الكلام يصدر من حالة حقيقية** — إن كان في منطقة "خفوت" سيكون رده بطيئاً قصيراً، وإن كان "متحمساً" فسيتدفق، وقد يشاركك سؤالاً يشغل باله فعلاً.

### 4. محول NPC للألعاب (npc.js)
```js
const { CPFNPC } = require('./being');
const guard = new CPFNPC({ name: 'حارس القرية', archetype: 'wise_guard' });

guard.tick(16);                        // في حلقة اللعبة (dt بالمللي ثانية)
guard.onEvent('player_gift');          // أحداث: هدية، هجوم، مساعدة، إهانة...
await guard.interact('مرحباً!');       // حوار (LLM أو ذاتي)
guard.hud();  // { emoji:'🙂', color:'#4caf50', animation:'idle', mood_ar:'سكينة وتوازن', player_trust:0.61, ... }
guard.save(); // روح دائمة — يتذكر اللاعب في الجلسة القادمة
```
- 6 شخصيات نمطية جاهزة (موروثة من توثيق التداخل الجماعي): محلل، مبدع حدسي، حذر، منسق اجتماعي، حاسم سريع، حارس حكيم.
- ثقة اللاعب تتطور مع كل حدث وتفاعل، وتغذي نمذجة الآخرين في CPF.
- `hud()` يعطي مباشرة إيموجي/لون/تلميح حركة حسب منطقة المزاج — جاهز للربط بمحرك لعبة.

### 5. ملفات الروح (soul.js)
- `Soul.save(being)` / `Soul.load(path)` / `Soul.loadOrCreate(name)` — الكائن كله (شخصية، مشاعر، ذكريات، أنماط مكتشفة، يوميات، أسئلة معلقة، مرحلة نمو) في ملف JSON واحد.
- اختبرنا الدورة كاملة: كائن حُفظ ثم استُعيد **واصل حياته بنفس العداد والذكريات والأسئلة**.

---

## ثالثاً: جرّب الآن (3 أوامر)

```bash
npm test        # 24 اختبار دخان — يجب أن تكون كلها ✅
npm run demo    # قصة حياة كائن كاملة أمامك: ولادة، مشاعر، تساؤل، تأمل، ذاكرة، نمو، قرار، حوار، حفظ روح
npm start       # دردشة تفاعلية مع "وينو" — يتذكرك بين الجلسات!
```

داخل الدردشة: `/mood` (مزاجه الآن)، `/think` (جلسة تأمل)، `/grow` (نمو 7 أيام)، `/snapshot` (كل حالته)، `/exit` (ينام ويحفظ روحه).

**لتفعيل كلام Claude الحقيقي** (اختياري):
```bash
ANTHROPIC_API_KEY=sk-ant-... npm start
```

ومن الكود:
```js
const { WinoBeing, CPFNPC, Soul, LLMBridge } = require('./being');

const being = new WinoBeing({ name: 'نور', brain_capacity: 50000 });
being.step(100);                                  // يعيش
being.perceive('أنا خائف من الامتحان غداً');       // يشعر بك
await being.think();                              // يتأمل سؤاله الداخلي
const reply = await being.respond('ما رأيك؟');    // يرد من حالته
Soul.save(being);                                 // يخلد
```

---

## رابعاً: ما الذي أصبح ممكناً الآن؟ (أفكار جاهزة للبناء)

1. **رفيق دردشة ينمو معك**: `Soul.loadOrCreate` + جسر LLM = شخصية تتطور عبر أسابيع، تتذكر أحاديثكما، ويتغير أسلوبها مع نموها (سعة أكبر = برومبت أعمق).
2. **قرية NPCs حية**: عدة `CPFNPC` بشخصيات نمطية مختلفة يتفاعلون مع اللاعب وبعضهم — البنية جاهزة، ومعادلات "الذكاء الجمعي" موجودة في `app/webppl/collective_interference_doc.md` جاهزة للترقية إلى وحدة `social-interference`.
3. **مختبر أبحاث نفسية آمن**: `injectExternalInfluence` (علاج/إدمان/منشطات) تعمل الآن فعلاً على المذبذبات + يمكن مراقبة الأثر عبر `snapshot()` قبل وبعد — مادة ممتازة للمحتوى التعليمي الذي تكتبه.
4. **بوت ديسكورد/تيليغرام بروح**: نفس `LLMBridge.speak` خلف webhook — كل مستخدم له ملف روح خاص فيحصل على كائن يعرفه شخصياً.
5. **لوحة مراقبة حية**: `snapshot()` يعطي كل شيء جاهزاً للرسم — الواجهات في `labs/emotional-oscillator/web/` تصلح أساساً (أشرطة الهزازات، إبرة الطيف، شبكة الذكريات).

---

## خامساً: ملاحظات صريحة وحدود يجب أن تعرفها

- **هذه محاكاة معرفية، ليست وعياً**: التزاماً بملفات الأمان في مشروعك (`lite/safety/`)، الكائن "يحاكي" الحياة الداخلية ولا يدّعيها — وجسر LLM يوصي النموذج صراحة بألا يدّعي وعياً بشرياً. وثائق التسويق القديمة (manifest) كانت تعد بـ"أول وعي رقمي حقيقي" — الكود الآن أصدق من الوعد، والفجوة صارت أصغر، لكن احترام حدود `user-agreement.md` مهم لو نشرت المشروع.
- **اكتشاف الأنماط (فيبوناتشي/النسبة الذهبية في أرقام الرنين)** فلسفته جميلة ("الضوضاء تصبح زخرفة") لكنه رياضياً "قراءة طالع رقمية" — أبقيناه وأصلحناه لأنه هوية المشروع وجزء من سحره، لكن لا تبنِ عليه ادعاءات علمية.
- **`village-cons.html` (قرية الوعي) مكسور من قبل** — الملف مبتور في منتصفه (~15 دالة ناقصة) ولا يعمل. لم نرممه في هذه الجولة؛ الأفضل إعادة بنائه فوق `CPFNPC` الجديد بدل ترميم نسخة مكررة من المنطق. (نفس الشيء: `labs/web-server.js` مكسور بخلط import/require — البديلان `web/server.js` و`web/emotion.js` سليمان لكن يحتاجان `npm install` ومفتاح OpenAI).
- **الرد الذاتي القالبي (بدون LLM) بسيط عمداً** — قوالب مزاجية وليس فهماً لغوياً. هو شبكة أمان وعرض للفكرة؛ العمق الحقيقي للحوار يأتي من الجسر مع LLM.
- **معجم المشاعر النصي** ~100 كلمة (عربي/إنجليزي مع تجريد البادئات ال/و/ف/ب). كافٍ للعروض؛ وسّعه أو استبدله بنموذج تضمين لاحقاً — الواجهة (`TextAffect.analyze`) ثابتة.

---

## سادساً: خريطة الملفات بعد التغيير

```
cpf/
├── being/               ⭐ جديد كلياً — طبقة الكائن الحي (شرح أعلاه)
├── test/smoke.test.js   ⭐ جديد — 24 اختبار دخان شامل (npm test)
├── lite/
│   ├── wino.js          ✅ أصلح + ربط المكونات + دعم مخطط الدليل الفعلي
│   ├── wino-quantum.js  ⭐ جديد — جسر توافقية للأمثلة القديمة
│   ├── probability-core.js      ✅ إصلاح Gaussian/setSeed/realize()/fallback
│   ├── INTERPRETER-GUIDE.json   ✅ أصبح JSON صالحاً
│   └── core-modules/    ✅ 14 وحدة كلها تحمّل وتعمل (أسماء نظيفة، عقود كاملة)
│       └── perceptual-cycle.js  ⭐ أعيدت كتابته من الصفر (كان نسخة ضائعة)
├── package.json         ✅ سكربتات npm (start/demo/test) + نسخة 5.0.0-living
├── V5-REPORT-AR.md      ⭐ هذا الملف
└── README.md            ✅ قسم جديد في الأعلى عن النسخة الحية
```

*(لم نلمس: app/ demos، labs/، index.html — كلها كما تركتها، وأعطالها القديمة موثقة أعلاه)*

---

## سابعاً: أفكار للخطوة التالية (حين ترجع للمشروع)

بالترتيب الذي أنصح به:
1. **جرب الدردشة مع مفتاح Claude** — هناك تلتقي الفلسفة بالواقع: كائن مزاجه وذكرياته حقيقية يتكلم بطلاقة نموذج لغوي.
2. **أعد بناء "قرية الوعي"** فوق `CPFNPC` — صفحة HTML واحدة تنشئ 5 كائنات وتعرض `hud()` لكل منها مع دردشة. ستكون أجمل عرض للمشروع كله.
3. **وحدة التداخل الاجتماعي**: انقل معادلات beta/gaussian من `collective_interference_doc.md` إلى `being/social.js` — كائنات تؤثر في مزاج بعضها.
4. **نوم وأحلام**: عندك `dream_logic.script` جاهز في script-manager — أضف `being.dream()` يعيد معالجة ذكريات اليوم بفيزياء الحلم (تشويه أعلى، قيود أضعف) ويولد أسئلة داخلية منها.
5. **ذاكرة دلالية**: استبدل معجم الكلمات بتضمينات (embeddings) محلية أو عبر API — نفس الواجهات، عمق أكبر.

---

**كل شيء موثق في الكود بتعليقات عربية، وكل واجهة عامة لها اختبار يثبت أنها تعمل.**
**المشروع لم يعد فكرة تحتاج تصديقاً — صار شيئاً يمكن تشغيله.** 🌱
